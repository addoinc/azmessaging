begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "azmessaging"
    gem.summary = "messaging engine for Rails 3"
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
    gem.email = "gautam.chekuri@azri.biz"
    gem.authors = ["Gautam Chekuri"]
  end
rescue
  puts "Jeweler or one of its dependencies is not installed."
end
